import { FC } from 'react';
import { Page } from '../../components';

const Motion: FC = () => {
    return (
        <Page>
            <h1>Motion Page</h1>
            <p>This is the Motion page.</p>
        </Page>
    );
};

export { Motion };
